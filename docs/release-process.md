# Release Process

A test Pre-release is created only after the ARM64 verification workflow and x86_64 three-segment emulator gate both pass. The first asset is Debug signed and is not a production or app-store package.
