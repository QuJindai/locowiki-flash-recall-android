# Privacy and Data Boundary

The APK does not request INTERNET permission. Voice enrollment stores normalized embeddings rather than enrollment recordings. Debug PCM injection is disabled by default, invisible in the UI, restricted to debuggable builds and fixed files under the app sandbox. No real user audio is committed or uploaded by CI.
